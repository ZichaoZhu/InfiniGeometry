# expn_实验英文简介

## 基本信息

- 状态：`planned`
- 研究问题：
- 结论边界：
- 唯一主变量：
- 官方基线提交：
- 实际运行提交：待运行

## 假设与对照

说明 K0 对照、被修改模块以及哪些输入严格禁止进入模型。训练域拟合结果不能写成泛化结论。

## 数据与配置

列出数据清单 SHA-256、样本 ID、读取语义、固定分辨率、seed、优化器、阶段日程、损失和评测频率。结构化值只在 `config.json` 中维护，本节只解释研究意图。

## 验收标准

写明同一 checkpoint 下 K3 与 K0 的比较规则、异常停止条件和进入下一实验的门槛。未实际运行不得填写为 `completed`。

## 结果与结论

只概括 `metrics/report.json` 的关键发现，不重复粘贴完整指标。明确失败、退化、资源限制和未验证事项。

## 复现命令

命令必须从仓库根目录执行，所有输出限制在服务器个人目录。

## 资产清单

引用 `artifacts/manifest.json`；禁止在文档中登记不存在的最终图、checkpoint 或点云。
